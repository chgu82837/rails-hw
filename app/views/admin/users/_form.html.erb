<%= form_for u, url: new_admin_user_path do |f| %>
  <% if u.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize u.errors.count, "error" %> prohibited this User from being saved:</h2>

      <ul>
      <% u.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.label :name %>:
  <%= f.text_field :name, class: "form-control" %>
  <br>
  <%= f.label :password %>:
  <%= f.password_field :password, class: "form-control" %>
  <br>
  <%= f.label :banned %>:
  <%= f.check_box :banned, class: "form-control" %>
  <br>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
